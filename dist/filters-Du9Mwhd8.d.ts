/**
 * SVG filter definitions — all scoped by uid prefix, zero cross-avatar collision.
 */
interface FilterIds {
    grain: string;
    glow: string;
    shadow: string;
    displace: string;
    blur: string;
}

export type { FilterIds as F };
